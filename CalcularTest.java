import javax.sql.rowset.CachedRowSet;

public class Calcular {
    private double variação;

    public void balança(Conta conta) {
        if (preço > i);
        preço == Caro;
             else (preço < i);
             preço == Barato;
    }

    public double getPrecoTotal() {
        return precoTotal;
    }
}

